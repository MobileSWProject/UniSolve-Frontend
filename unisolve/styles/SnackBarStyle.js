import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  snackbarContainer: {
    position: "absolute",
    zIndex: 999,
    top: "15%",
    width: "80%",
    alignSelf: "center",
  },
  snackbar: {
    borderRadius: 15,
  },
});
